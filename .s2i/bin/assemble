#!/bin/bash
set -e

shopt -s dotglob
echo "---> Installing application source..."
DEST=`pwd`
cd /tmp/src/
curl https://getcomposer.org/installer | php

rmdir app/sites/default/files/tmp

mv /tmp/src/app/* $DEST

# Fix source directory permissions
fix-permissions $DEST
