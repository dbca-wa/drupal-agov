#!/bin/bash

set -e

shopt -s dotglob
echo "---> Installing application source..."
DEST=`pwd`
cd /tmp/src/
yum install drush
./build.sh
rmdir drupal/sites/default/files/tmp

mv /tmp/src/drupal/* $DEST

# Fix source directory permissions
fix-permissions $DEST
